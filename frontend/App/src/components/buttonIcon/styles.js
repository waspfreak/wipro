import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonBackground: {
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 50,
    height: 28,
    width: 28
  }
});
